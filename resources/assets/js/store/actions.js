import * as types from './mutations-types';

// const actions = {
//   setCartItemsAmount (context, number) {
//     context.commit(types.SET_CARTITEMSAMOUNT_MUTATION, number)
//   }
// }

export default actions;
